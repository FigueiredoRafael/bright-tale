# ─── Admin affiliate pages (Phase 2C) ─────────────────────────────────
# API_URL — local dev: http://localhost:3001 / production: https://api.brighttale.io
API_URL=http://localhost:3001

# INTERNAL_API_KEY — MUST match apps/api's INTERNAL_API_KEY exactly.
# INTERNAL_API_KEY=dev-shared-secret
