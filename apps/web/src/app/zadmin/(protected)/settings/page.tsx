import { ComingSoon } from '../_components/ComingSoon'

export default function AdminSettingsPage() {
  return (
    <ComingSoon
      title="Configurações do admin"
      card="M-018-admin-redesign"
      description="Pricing config (block size do top-up, free tier credits, validade do bônus), permissions (delegar reset de tokens pro support, etc.), SLAs do support, thresholds da margem, cotação USD."
    />
  )
}
