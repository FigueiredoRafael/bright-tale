NODE_ENV=development

# API — for admin dashboard health check + plan config proxy routes
API_URL=http://localhost:3001
INTERNAL_API_KEY=

# Supabase — public (safe to expose in browser)
# Local dev values below (same for everyone via npm run db:start)
# Staging/prod: replace with values from Supabase dashboard
NEXT_PUBLIC_SUPABASE_URL=http://127.0.0.1:54321
NEXT_PUBLIC_SUPABASE_ANON_KEY=sb_publishable_ACJWlzQHlZjBrEguHvfOxg_3BJgxAaH

# Supabase — service role (server-side ONLY, for /zadmin panel)
SUPABASE_URL=http://127.0.0.1:54321
SUPABASE_SERVICE_ROLE_KEY=sb_secret_N7UND0UgjKTVK-Uodkm0Hg_xSvEMPvz

# Admin panel slug (change to obscure the URL in prod)
NEXT_PUBLIC_ADMIN_SLUG=zadmin

# Axiom — client-side logging
NEXT_PUBLIC_AXIOM_TOKEN=
NEXT_PUBLIC_AXIOM_DATASET=brighttale-web

# PostHog — client-side analytics (posthog.com, free 1M events/mo)
NEXT_PUBLIC_POSTHOG_KEY=
NEXT_PUBLIC_POSTHOG_HOST=https://us.i.posthog.com

# Stripe — publishable key (safe to expose in browser)
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=
